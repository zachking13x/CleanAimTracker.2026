<Window x:Class="CleanAimTracker.Windows.ConverterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sensitivity Converter"
        Height="520"
        Width="680"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackground}"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="SectionLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource LabelText}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0 0 0 4"/>
        </Style>
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0 0 0 12"/>
        </Style>

        <!-- Full dark-theme ComboBox template -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground"      Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Background"      Value="#252525"/>
            <Setter Property="BorderBrush"     Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Margin"          Value="0 0 0 10"/>
            <Setter Property="Padding"         Value="8 0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Margin="8 0 0 0"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      TextBlock.Foreground="{TemplateBinding Foreground}"/>
                                    <ToggleButton Grid.Column="1"
                                                  Focusable="False"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                                  ClickMode="Press">
                                        <Path Data="M 0 0 L 5 5 L 10 0"
                                              Stroke="{DynamicResource MutedText}"
                                              StrokeThickness="1.5"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border Background="#252525"
                                        BorderBrush="#3A3A3A"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        Padding="2"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer MaxHeight="200">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground"  Value="{DynamicResource PrimaryText}"/>
                        <Setter Property="Background"  Value="Transparent"/>
                        <Setter Property="Padding"     Value="10 7"/>
                        <Setter Property="Cursor"      Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBd"
                                            Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}"
                                            CornerRadius="4">
                                        <TextBlock Text="{TemplateBinding Content}"
                                                   Foreground="{TemplateBinding Foreground}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="#2A3A42"/>
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                            <Setter Property="Foreground" Value="Black"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground"      Value="{DynamicResource PrimaryText}"/>
            <Setter Property="Background"      Value="#252525"/>
            <Setter Property="BorderBrush"     Value="#3A3A3A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Padding"         Value="8 4"/>
            <Setter Property="Margin"          Value="0 0 0 10"/>
            <Setter Property="CaretBrush"      Value="{DynamicResource AccentBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Sensitivity Converter"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryText}"
                   Margin="0 0 0 16"/>

        <!-- Two-panel input -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- SOURCE panel -->
            <Border Grid.Column="0" Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="FROM" FontSize="12" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0 0 0 10"/>

                    <TextBlock Text="Game:" Style="{StaticResource SectionLabel}"/>
                    <ComboBox x:Name="SourceGameCombo" SelectionChanged="OnInputChanged"/>

                    <TextBlock Text="DPI:" Style="{StaticResource SectionLabel}"/>
                    <TextBox x:Name="DpiInput" TextChanged="OnInputChanged"/>

                    <TextBlock Text="In-game Sensitivity:" Style="{StaticResource SectionLabel}"/>
                    <TextBox x:Name="SensInput" TextChanged="OnInputChanged"/>

                    <TextBlock Text="cm/360:" Style="{StaticResource SectionLabel}"/>
                    <TextBlock x:Name="SourceCm360Text"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16" FontWeight="SemiBold"
                               Text="--"/>
                </StackPanel>
            </Border>

            <!-- Arrow -->
            <TextBlock Grid.Column="1"
                       Text="→"
                       FontSize="28"
                       Foreground="{DynamicResource AccentBrush}"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>

            <!-- TARGET panel -->
            <Border Grid.Column="2" Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="TO" FontSize="12" FontWeight="Bold"
                               Foreground="{DynamicResource AccentBrush}" Margin="0 0 0 10"/>

                    <TextBlock Text="Game:" Style="{StaticResource SectionLabel}"/>
                    <ComboBox x:Name="TargetGameCombo" SelectionChanged="OnInputChanged"/>

                    <TextBlock Text="Recommended DPI:" Style="{StaticResource SectionLabel}"/>
                    <TextBlock x:Name="TargetDpiText"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="14"
                               Text="--"
                               Margin="0 0 0 10"/>

                    <TextBlock Text="Converted Sensitivity:" Style="{StaticResource SectionLabel}"/>
                    <TextBlock x:Name="ResultSensText"
                               Foreground="{DynamicResource AccentBrush}"
                               FontSize="26"
                               FontWeight="Bold"
                               Text="--"/>

                    <TextBlock Text="Same cm/360:" Style="{StaticResource SectionLabel}" Margin="0 8 0 4"/>
                    <TextBlock x:Name="TargetCm360Text"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="14"
                               Text="--"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Status / error message -->
        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Foreground="#FF6B6B"
                   FontSize="12"
                   Margin="0 0 0 10"
                   Text=""/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CopyButton"
                    Content="📋 Copy Sensitivity"
                    Width="160"
                    Height="34"
                    Margin="0 0 10 0"
                    Click="CopyResult_Click"
                    Background="{DynamicResource AccentBrush}"
                    Foreground="White"
                    FontWeight="SemiBold"
                    BorderThickness="0"/>
            <Button Content="Close"
                    Width="80"
                    Height="34"
                    Click="Close_Click"
                    Background="{DynamicResource CardBackground}"
                    Foreground="{DynamicResource PrimaryText}"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource AccentBrush}"/>
        </StackPanel>
    </Grid>
</Window>
