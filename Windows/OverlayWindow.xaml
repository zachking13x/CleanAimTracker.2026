<Window x:Class="CleanAimTracker.Windows.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        Width="300"
        Height="60"
        ShowInTaskbar="False"
        Loaded="Overlay_Loaded"
        Icon="{StaticResource AppLogo}"
        LocationChanged="Overlay_LocationChanged">

    <Window.Resources>

        <!-- Minimal, premium overlay button style -->
        <Style x:Key="OverlayButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border Background="#222222"
            CornerRadius="8"
            Opacity="0.85"
            Padding="10"
            MouseDown="OverlayDrag">

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

            <Button Content="Start"
                    Click="Start_Click"
                    Style="{StaticResource OverlayButtonStyle}"/>

            <Button Content="Stop"
                    Click="Stop_Click"
                    Style="{StaticResource OverlayButtonStyle}"/>

            <Button Content="Recommend"
                    Width="100"
                    Click="Recommend_Click"
                    Style="{StaticResource OverlayButtonStyle}"/>

        </StackPanel>

    </Border>

</Window>
