<Window x:Class="CleanAimTracker.Windows.ReminderPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="See You Tomorrow"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="{DynamicResource AppBackground}"
        ShowInTaskbar="False">
    <!-- TASK-4.2: styled in-app retention prompt replacing the raw Windows
         MessageBox ("gray system dialog" — DESIGN_SPEC red-flag list). -->
    <Border BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="10"
            Background="{DynamicResource CardBackground}">
        <StackPanel Margin="24,20,24,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="🔥" FontSize="18" Margin="0,0,8,0"/>
                <TextBlock x:Name="TitleText" Text="See you tomorrow?"
                           FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryText}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <TextBlock x:Name="MessageText"
                       TextWrapping="Wrap" FontSize="13" LineHeight="20"
                       Foreground="{DynamicResource SecondaryText}"
                       Margin="0,0,0,18"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Not now"
                        Click="NotNow_Click"
                        Background="Transparent"
                        Foreground="{DynamicResource MutedText}"
                        BorderBrush="{DynamicResource BorderSubtle}"
                        BorderThickness="1"
                        Padding="16,8"
                        Margin="0,0,10,0"
                        Cursor="Hand"/>
                <Button Content="Remind me"
                        Click="RemindMe_Click"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="#07090C"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Padding="18,8"
                        Cursor="Hand"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
