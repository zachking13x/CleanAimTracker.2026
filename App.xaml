<Application x:Class="CleanAimTracker.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <BitmapImage x:Key="AppLogo" UriSource="Assets/BCO.21dff569-623b-4839-b0ad-1cce7c533742.png"/>
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

            <!--
                ══════════════════════════════════════════════════════════════
                DESIGN SYSTEM RESOURCES — from DESIGN_SPEC.md
                Colors, brushes, and base styles are defined in DarkTheme.xaml
                and merged above. Aliases kept here for discoverability.

                Key resources available app-wide (defined in DarkTheme.xaml):
                  Backgrounds : BgBase, BgSurface, BgElevated, BgOverlay
                  Accents     : AccentPrimary, AccentWarm, AccentGreen,
                                AccentOrange, AccentPurple, AccentGold
                  Text        : TextPrimary, TextSecondary, TextMuted
                  Card tints  : CardBgCyan, CardBgAmber, CardBgGreen, CardBgPurple
                  Borders     : BorderSubtle
                  Styles      : MetricCardStyle, MetricLabelStyle,
                                MetricValueStyle, PrimaryButtonStyle
                ══════════════════════════════════════════════════════════════
            -->

            <Style x:Key="GlowButton" TargetType="Button">
                <Setter Property="Background" Value="#222"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderBrush" Value="#66CCFF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,4"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderBrush" Value="#00AFFF"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="#00AFFF" BlurRadius="15" ShadowDepth="0" Opacity="0.8"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
                <Setter Property="Background" Value="{DynamicResource CardBackground}" />
                <Setter Property="Padding" Value="6,3" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#33373D" />
                        <Setter Property="Foreground" Value="White" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#0078D4" />
                        <Setter Property="Foreground" Value="White" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{DynamicResource CardBackground}" />
                <Setter Property="Foreground" Value="{DynamicResource PrimaryText}" />
                <Setter Property="BorderBrush" Value="#444" />
                <Setter Property="Padding" Value="4,2" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>

</Application>
