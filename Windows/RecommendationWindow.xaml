<Window x:Class="CleanAimTracker.Windows.RecommendationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sensitivity Recommendation"
        Width="520"
        Height="680"
        Background="{DynamicResource AppBackground}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">

            <!-- Header -->
            <TextBlock Text="Recommended Settings"
                       Foreground="{DynamicResource PrimaryText}"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,12"/>

            <!-- Sensitivity -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Sensitivity"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="SensValue"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="14"
                               Margin="0,4,0,0"/>

                    <TextBlock x:Name="SensRange"
                               Foreground="{DynamicResource SecondaryText}"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- DPI -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="DPI"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="DpiValue"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="14"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- cm/360 -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="cm/360"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="Cm360Value"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="14"
                               Margin="0,4,0,0"/>

                    <TextBlock x:Name="Cm360Verdict"
                               Foreground="{DynamicResource SecondaryText}"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- Confidence -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Confidence"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="ConfidenceValue"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="14"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Explanation -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Explanation"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="ExplanationText"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource SecondaryText}"
                               FontSize="13"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Tips -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Tips"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <ItemsControl x:Name="TipsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="•  {Binding}"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource SecondaryText}"
                                           Margin="0,4,0,0"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Trend -->
            <Border Background="{DynamicResource CardBackground}"
                    CornerRadius="8"
                    Padding="12"
                    Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Trend Analysis"
                               Foreground="{DynamicResource PrimaryText}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <TextBlock x:Name="TrendText"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource SecondaryText}"
                               FontSize="13"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Close -->
            <Button Content="Close"
                    Click="Close_Click"
                    Style="{StaticResource GlowButton}"
                    HorizontalAlignment="Center"
                    Width="120"
                    Margin="0,12,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Window>
