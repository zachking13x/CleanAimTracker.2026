<Window x:Class="CleanAimTracker.Windows.AddProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Profile"
        Height="400"
        Width="600"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <StackPanel>

            <TextBlock Text="Add New Profile"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0 0 0 20"/>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="Profile Name:" Width="120" VerticalAlignment="Center"/>
                <TextBox Width="300"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                <TextBlock Text="DPI:" Width="120" VerticalAlignment="Center"/>
                <TextBox Width="300"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0 0 0 20">
                <TextBlock Text="Sensitivity:" Width="120" VerticalAlignment="Center"/>
                <TextBox Width="300"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save"
                        Width="100"
                        Margin="0 0 10 0"
                        Click="Save_Click"/>
                <Button Content="Cancel"
                        Width="100"
                        Click="Cancel_Click"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</Window>
